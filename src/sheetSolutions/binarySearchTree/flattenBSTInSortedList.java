package sheetSolutions.binarySearchTree;

public class flattenBSTInSortedList {
    static Node flatten(Node root){
        Node curr=root,prev;
        while(curr!=null){

        }
        return curr;
    }
}
